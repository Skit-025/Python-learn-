Name="Aditya" #for short hand
Namei='aditya'#for same but another way
name= '''Aditya''' #for multiline text(string)
#therefore we can create a string in 3 ways
#string is immutable

#Slicing a String by the concept of index from starting the start index =0 but in reverse we consider index=-1 here length=6
#to get the length of a string we write or use the method len()
print(len(Name))
slicedname=name[0:3] # Starting from 0th character it will print upto 3 letters 
character1=name[1:]
print(slicedname)
print(character1)